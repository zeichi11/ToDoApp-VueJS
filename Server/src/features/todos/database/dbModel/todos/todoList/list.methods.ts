import { Document, Schema } from 'mongoose'
import { IList, IListDocument } from './list.type'

// export async function getAllItems(this: IItemDocument): Promise<IItem[]> {
//   return this.boards
// }